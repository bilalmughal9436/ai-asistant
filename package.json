{
  "name": "open-executive",
  "version": "0.3.0",
  "private": true,
  "description": "Open Executive repository entrypoint for Vercel",
  "scripts": {
    "build": "npm run build --prefix packages/ui",
    "test": "npm run test --prefix packages/ui"
  },
  "dependencies": {
    "next": "16.3.5"
  },
  "engines": {
    "node": ">=22 <25"
  }
}
